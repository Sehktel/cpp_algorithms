# Примеры программ для strace и ltrace

## 📚 Описание

Эта директория содержит примеры программ для изучения инструментов strace и ltrace. Каждая программа демонстрирует различные типы системных вызовов и библиотечных функций.

## 📁 Файлы

### file_operations.cpp
Программа для изучения файловых системных вызовов:
- `open()` / `openat()` - открытие файлов
- `read()` - чтение из файлов
- `write()` - запись в файлы
- `stat()` - информация о файлах
- `opendir()` / `readdir()` - работа с директориями

**Компиляция:**
```bash
g++ -o file_operations file_operations.cpp
```

**Использование со strace:**
```bash
# Проследить все системные вызовы
strace ./file_operations read /etc/passwd

# Проследить только файловые операции
strace -e trace=file ./file_operations read /etc/passwd

# Сохранить вывод в файл
strace -o trace.log ./file_operations read /etc/passwd
```

### network_operations.cpp
Программа для изучения сетевых системных вызовов:
- `socket()` - создание сокета
- `connect()` - подключение к серверу
- `send()` / `recv()` - отправка/получение данных
- `bind()` / `listen()` / `accept()` - серверные операции

**Компиляция:**
```bash
g++ -o network_operations network_operations.cpp
```

**Использование со strace:**
```bash
# Проследить сетевые вызовы
strace -e trace=network ./network_operations 127.0.0.1 8080 "Hello"

# Проследить все системные вызовы
strace ./network_operations 127.0.0.1 8080 "Hello"
```

### library_calls.cpp
Программа для изучения библиотечных вызовов с ltrace:
- Математические функции (`sin`, `cos`, `sqrt`, `pow`)
- Строковые функции (`strcat`, `strlen`, `strstr`)
- Функции времени (`time`, `localtime`, `strftime`)
- Функции памяти (`malloc`, `calloc`, `memcpy`, `free`)

**Компиляция:**
```bash
g++ -o library_calls library_calls.cpp -lm
```

**Использование с ltrace:**
```bash
# Проследить все библиотечные вызовы
ltrace ./library_calls all

# Проследить только математические функции
ltrace -e sin,cos,sqrt,pow ./library_calls math

# Сохранить вывод в файл
ltrace -o trace.log ./library_calls all
```

## 🚀 Практические задания

### Задание 1: Анализ файловых операций
1. Запустите `strace` на `file_operations`
2. Найдите все вызовы `open()` и `read()`
3. Определите, какие файлы открывает программа
4. Проанализируйте параметры системных вызовов

### Задание 2: Анализ сетевых операций
1. Запустите простой TCP сервер (например, из темы 8)
2. Запустите `strace` на `network_operations`
3. Найдите вызовы `socket()`, `connect()`, `send()`, `recv()`
4. Проанализируйте параметры сетевых вызовов

### Задание 3: Анализ библиотечных вызовов
1. Запустите `ltrace` на `library_calls`
2. Найдите вызовы математических функций
3. Определите параметры и возвращаемые значения
4. Сравните с ожидаемыми результатами

## 📖 Дополнительные примеры

Дополнительные примеры будут добавлены по мере развития курса.

---

**Статус:** ✅ Базовые примеры созданы  
**Последнее обновление:** 2025

