# Тема 12: strace/ltrace - "Рентген" программы

## 📚 Описание

strace и ltrace - инструменты для отслеживания системных вызовов и вызовов библиотечных функций. Критичны для:
- Анализа поведения программ
- Обнаружения подозрительной активности
- Понимания взаимодействия программ с системой
- Отладки проблем производительности

## 🎯 Цели обучения

- Использование strace для отслеживания системных вызовов
- Использование ltrace для отслеживания библиотечных вызовов
- Анализ вывода strace/ltrace
- Понимание системных вызовов Linux
- Применение для анализа безопасности

## 🔗 Связи с другими темами

### Предыдущие темы
- [Тема 3: Shared libraries](../../01_cpp_fundamentals/03_shared_libraries/) - понимание загрузки библиотек
- [Тема 7: File I/O](../../01_cpp_fundamentals/07_file_io/) - анализ файловых операций
- [Тема 9: GDB отладка](../09_gdb_disasm/) - комбинация с отладчиком
- [Тема 8: TCP Client-Server](../../02_networking/08_tcp_client_server/) - анализ сетевых вызовов

### Следующие темы
- [Тема 13: Filesystem Forensics](../../04_filesystem/13_filesystem_forensics/) - анализ файловых операций
- [Тема 21: DFIR](../../07_forensics/21_dfir/) - анализ поведения программ

## 📁 Структура лабораторной работы

```
12_strace_ltrace/
├── README.md                    # Этот файл
└── examples/                    # Примеры использования ✅
```

## 🚀 Запуск

### Установка инструментов

```bash
# Ubuntu/Debian
sudo apt-get install strace ltrace

# Fedora
sudo dnf install strace ltrace

# macOS (через Homebrew)
brew install strace ltrace
```

### Базовое использование strace

```bash
# Проследить все системные вызовы программы
strace ./program_name

# Проследить только определенные типы вызовов
strace -e trace=file ./program_name      # Только файловые операции
strace -e trace=network ./program_name   # Только сетевые операции
strace -e trace=process ./program_name   # Только операции с процессами

# Сохранить вывод в файл
strace -o trace.log ./program_name

# Показать время выполнения каждого вызова
strace -T ./program_name

# Подсчитать системные вызовы
strace -c ./program_name
```

### Базовое использование ltrace

```bash
# Проследить все библиотечные вызовы
ltrace ./program_name

# Проследить только определенные функции
ltrace -e malloc,free ./program_name

# Сохранить вывод в файл
ltrace -o trace.log ./program_name

# Показать время выполнения каждого вызова
ltrace -T ./program_name

# Подсчитать библиотечные вызовы
ltrace -c ./program_name
```

## 📖 Теоретическая часть

### Что такое strace?

**strace** - это инструмент для отслеживания системных вызовов (system calls) программы. Системные вызовы - это интерфейс между пользовательским пространством и ядром операционной системы.

**Основные системные вызовы:**
- `open()` / `read()` / `write()` / `close()` - работа с файлами
- `socket()` / `connect()` / `send()` / `recv()` - сетевые операции
- `fork()` / `exec()` / `wait()` - управление процессами
- `mmap()` / `munmap()` - работа с памятью
- `stat()` / `chmod()` / `chown()` - управление файлами

### Что такое ltrace?

**ltrace** - это инструмент для отслеживания вызовов библиотечных функций (library calls). В отличие от strace, ltrace отслеживает вызовы функций из динамических библиотек.

**Основные библиотечные функции:**
- `malloc()` / `free()` - управление памятью
- `strcpy()` / `strcat()` / `strlen()` - работа со строками
- `sin()` / `cos()` / `sqrt()` - математические функции
- `printf()` / `scanf()` - ввод/вывод

### Разница между strace и ltrace

| strace | ltrace |
|--------|--------|
| Системные вызовы (ядро) | Библиотечные функции (пользовательское пространство) |
| `open()`, `read()`, `write()` | `fopen()`, `fread()`, `fwrite()` |
| Низкоуровневые операции | Высокоуровневые операции |
| Медленнее (перехват на уровне ядра) | Быстрее (перехват на уровне библиотек) |

### Практическое применение

**strace полезен для:**
- Отладки проблем с файлами и сетью
- Понимания, какие системные ресурсы использует программа
- Обнаружения подозрительной активности (например, попытки открыть несуществующие файлы)
- Анализа производительности (какие системные вызовы занимают больше времени)

**ltrace полезен для:**
- Отладки проблем с библиотеками
- Понимания, какие библиотечные функции вызываются
- Обнаружения утечек памяти (отслеживание `malloc`/`free`)
- Анализа использования библиотек

## 🛠 Практическая часть

### Пример 1: Анализ файловых операций

См. [examples/file_operations.cpp](./examples/file_operations.cpp)

```bash
# Компиляция
g++ -o file_operations examples/file_operations.cpp

# Запуск со strace
strace -e trace=file ./file_operations read /etc/passwd

# Анализ вывода:
# openat(AT_FDCWD, "/etc/passwd", O_RDONLY) = 3
# read(3, "root:x:0:0:root:/root:/bin/bash\n"..., 4096) = 1234
# close(3) = 0
```

### Пример 2: Анализ сетевых операций

См. [examples/network_operations.cpp](./examples/network_operations.cpp)

```bash
# Компиляция
g++ -o network_operations examples/network_operations.cpp

# Запуск сервера (в другом терминале)
# ./server 8080

# Запуск клиента со strace
strace -e trace=network ./network_operations 127.0.0.1 8080 "Hello"

# Анализ вывода:
# socket(AF_INET, SOCK_STREAM, IPPROTO_TCP) = 3
# connect(3, {sa_family=AF_INET, sin_port=htons(8080), sin_addr=inet_addr("127.0.0.1")}, 16) = 0
# send(3, "Hello", 5, 0) = 5
# recv(3, "Echo: Hello", 11, 0) = 11
```

### Пример 3: Анализ библиотечных вызовов

См. [examples/library_calls.cpp](./examples/library_calls.cpp)

```bash
# Компиляция
g++ -o library_calls examples/library_calls.cpp -lm

# Запуск с ltrace
ltrace ./library_calls math

# Анализ вывода:
# sin(3.141590) = 0.000000
# cos(3.141590) = -1.000000
# sqrt(2.718280) = 1.648721
# pow(3.141590, 2) = 9.869600
```

### Практические задания

1. **Анализ файловых операций:**
   - Запустите `strace` на программе, которая читает файл
   - Найдите все вызовы `open()` и определите, какие файлы открываются
   - Проанализируйте параметры `read()` и `write()`

2. **Анализ сетевых операций:**
   - Запустите TCP клиент со `strace`
   - Найдите вызовы `socket()`, `connect()`, `send()`, `recv()`
   - Проанализируйте параметры сетевых вызовов

3. **Обнаружение утечек памяти:**
   - Используйте `ltrace` для отслеживания `malloc()` и `free()`
   - Найдите случаи, когда `malloc()` вызывается без соответствующего `free()`

4. **Анализ производительности:**
   - Используйте `strace -T` для измерения времени системных вызовов
   - Определите, какие вызовы занимают больше всего времени

## 🔍 Вопросы для самопроверки

1. В чем разница между strace и ltrace?
2. Какие системные вызовы используются для работы с файлами?
3. Как найти все файлы, которые открывает программа?
4. Как определить, какие библиотечные функции вызываются чаще всего?
5. Как использовать strace для обнаружения подозрительной активности?
6. В чем разница между `open()` и `fopen()` с точки зрения strace/ltrace?
7. Как использовать strace для анализа производительности программы?

## 📚 Дополнительные ресурсы

- [strace manual](https://man7.org/linux/man-pages/man1/strace.1.html)
- [ltrace manual](https://man7.org/linux/man-pages/man1/ltrace.1.html)

---

**Статус:** ✅ Базовые материалы и примеры созданы  
**Последнее обновление:** 2025

