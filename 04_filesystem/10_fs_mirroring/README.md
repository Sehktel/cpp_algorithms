# Тема 10: Прозрачное зеркалирование файловой системы

## 📚 Описание

Прозрачное зеркалирование файловой системы позволяет отслеживать все операции с файлами. Критично для:
- Мониторинга файловой системы
- Обнаружения подозрительной активности
- Создания систем аудита
- Понимания работы файловых систем

## 🎯 Цели обучения

- Понимание механизмов файловых систем
- Использование FUSE (Filesystem in Userspace)
- Реализация прозрачного зеркалирования
- Мониторинг операций с файлами
- Применение для безопасности

## 🔗 Связи с другими темами

### Предыдущие темы
- [Тема 7: File I/O](../../01_cpp_fundamentals/07_file_io/) - понимание работы с файлами

### Следующие темы
- [Тема 13: Filesystem Forensics](../13_filesystem_forensics/) - анализ файловой системы
- [Тема 16: auditd/journald](../../06_security_tools/16_auditd_journald/) - аудит файловых операций

## 📁 Структура лабораторной работы

```
10_fs_mirroring/
├── README.md                    # Этот файл
└── examples/                    # Примеры реализации (в разработке)
```

## 🚀 Запуск

### Установка зависимостей

```bash
# Ubuntu/Debian
sudo apt-get install libfuse-dev fuse

# Fedora
sudo dnf install fuse-devel fuse

# macOS
brew install macfuse
```

### Компиляция примеров

```bash
cd examples
g++ -o fs_mirror fs_mirror.cpp `pkg-config fuse --cflags --libs`
```

### Запуск

```bash
# Создать директории для зеркалирования
mkdir -p /tmp/original /tmp/mirror

# Запустить зеркалирование
./fs_mirror /tmp/original /tmp/mirror

# В другом терминале работать с /tmp/original
# Все операции будут логироваться
```

## 📖 Теоретическая часть

### Что такое прозрачное зеркалирование?

Прозрачное зеркалирование файловой системы - это техника, при которой все операции с файлами (чтение, запись, удаление) отслеживаются и логируются без изменения поведения файловой системы для пользователя.

### FUSE (Filesystem in Userspace)

FUSE позволяет создавать файловые системы в пользовательском пространстве без модификации ядра. Это идеально для:
- Прототипирования файловых систем
- Создания виртуальных файловых систем
- Мониторинга операций с файлами
- Реализации прозрачного зеркалирования

### Архитектура зеркалирования

```
Приложение
    |
    v
FUSE Kernel Module
    |
    v
Наша программа (fs_mirror)
    |
    +---> Оригинальная ФС (read-only)
    |
    +---> Зеркальная ФС (write)
    |
    +---> Логирование операций
```

### Основные операции FUSE

- `getattr()` - получение атрибутов файла
- `readdir()` - чтение директории
- `open()` - открытие файла
- `read()` - чтение из файла
- `write()` - запись в файл
- `create()` - создание файла
- `unlink()` - удаление файла

## 🛠 Практическая часть

### Пример 1: Базовое зеркалирование

См. [examples/fs_mirror.cpp](./examples/fs_mirror.cpp) - базовая реализация зеркалирования с логированием.

### Пример 2: Зеркалирование с фильтрацией

См. [examples/fs_mirror_filtered.cpp](./examples/fs_mirror_filtered.cpp) - зеркалирование только определенных типов файлов.

### Практические задания

1. **Базовое зеркалирование:**
   - Реализовать простое зеркалирование всех операций
   - Логировать все операции в файл

2. **Фильтрация операций:**
   - Зеркалировать только операции записи
   - Игнорировать операции чтения

3. **Анализ активности:**
   - Подсчитать количество операций каждого типа
   - Определить наиболее активные файлы

## 🔍 Вопросы для самопроверки

1. В чем разница между прозрачным зеркалированием и обычным копированием?
2. Почему FUSE удобен для создания файловых систем?
3. Какие операции файловой системы критичны для безопасности?
4. Как можно использовать зеркалирование для обнаружения подозрительной активности?
5. В чем преимущества пользовательских файловых систем перед модификацией ядра?

## 📚 Дополнительные ресурсы

- [FUSE Documentation](https://github.com/libfuse/libfuse)
- [Linux Filesystem Hierarchy](https://en.wikipedia.org/wiki/Filesystem_Hierarchy_Standard)

---

**Статус:** ✅ Базовые материалы созданы  
**Последнее обновление:** 2025

