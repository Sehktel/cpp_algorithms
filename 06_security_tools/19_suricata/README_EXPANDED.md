# Расширенные материалы: Suricata

## 🚀 Базовое использование

### Установка

```bash
# Ubuntu/Debian
sudo apt-get install suricata

# Настройка
sudo suricata-update
```

### Запуск

```bash
# Тестовый режим
suricata -c /etc/suricata/suricata.yaml -S rules.rules -i eth0

# Режим IDS
sudo suricata -c /etc/suricata/suricata.yaml -i eth0

# Просмотр логов
tail -f /var/log/suricata/fast.log
```

## 📖 Основные концепции

### Правила Suricata

```suricata
alert tcp any any -> any 80 (msg:"HTTP Request"; content:"GET"; sid:1000001;)
```

**Компоненты:**
- `alert` - действие
- `tcp` - протокол
- `any any -> any 80` - трафик
- `msg` - сообщение
- `content` - содержимое для поиска

### Режимы работы

- **IDS** - обнаружение (passive)
- **IPS** - предотвращение (active)

---

**Примечание:** Это расширенный материал. Основной README содержит структуру темы.

