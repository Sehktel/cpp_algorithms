# Тема 14: Сборка минимального ядра Linux

## 📚 Описание

Понимание процесса сборки ядра Linux необходимо для:
- Понимания работы операционной системы
- Разработки модулей ядра
- Анализа безопасности ядра
- Кастомизации системы

## 🎯 Цели обучения

- Конфигурация ядра Linux
- Сборка минимального ядра
- Понимание компонентов ядра
- Работа с initramfs
- Создание загрузочного образа

## 🔗 Связи с другими темами

### Предыдущие темы
- [Тема 3: Shared libraries](../../01_cpp_fundamentals/03_shared_libraries/) - понимание загрузки модулей

### Следующие темы
- [Тема 15: Kernel modules](../15_kernel_module/) - разработка модулей ядра
- [Тема 20: SELinux/AppArmor](../../06_security_tools/20_selinux_apparmor/) - механизмы безопасности ядра

## 📁 Структура лабораторной работы

```
14_minimal_kernel/
├── README.md                    # Этот файл
└── build_scripts/              # Скрипты сборки (в разработке)
```

## 🚀 Запуск

### Требования

```bash
# Установка необходимых инструментов
sudo apt-get install build-essential libncurses-dev bison flex libssl-dev libelf-dev

# Или для Fedora
sudo dnf install gcc make ncurses-devel bison flex openssl-devel elfutils-libelf-devel
```

### Скачивание исходников ядра

```bash
# Скачать последнюю стабильную версию
cd /usr/src
wget https://cdn.kernel.org/pub/linux/kernel/v6.x/linux-6.1.tar.xz
tar -xf linux-6.1.tar.xz
cd linux-6.1
```

### Конфигурация ядра

```bash
# Минимальная конфигурация
make defconfig

# Или интерактивная конфигурация
make menuconfig

# Или использовать конфигурацию текущего ядра
cp /boot/config-$(uname -r) .config
make olddefconfig
```

### Сборка ядра

```bash
# Сборка (используйте количество ядер CPU)
make -j$(nproc)

# Сборка модулей
make modules

# Установка модулей
sudo make modules_install

# Установка ядра
sudo make install
```

## 📖 Теоретическая часть

### Компоненты ядра Linux

**1. Процессор и память:**
- Управление процессами и потоками
- Управление памятью (MMU, страничная память)
- Планировщик задач

**2. Файловая система:**
- VFS (Virtual File System)
- Поддержка различных файловых систем
- Кэширование

**3. Сеть:**
- Сетевой стек (TCP/IP)
- Сетевые драйверы
- Firewall и фильтрация пакетов

**4. Устройства:**
- Драйверы устройств
- Подсистема ввода/вывода
- Управление прерываниями

### Минимальная конфигурация

Для создания минимального ядра нужно включить только:
- Базовую поддержку процессора
- Минимальную файловую систему
- Базовые драйверы
- Initramfs для загрузки

### Initramfs

Initramfs (Initial RAM File System) - это временная файловая система, загружаемая в память перед монтированием основной файловой системы. Содержит:
- Базовые утилиты
- Драйверы для загрузки
- Скрипты инициализации

## 🛠 Практическая часть

### Пример 1: Минимальная конфигурация

См. [build_scripts/minimal_config.sh](./build_scripts/minimal_config.sh) - скрипт для создания минимальной конфигурации.

### Пример 2: Сборка с initramfs

См. [build_scripts/build_with_initramfs.sh](./build_scripts/build_with_initramfs.sh) - скрипт для сборки с initramfs.

### Практические задания

1. **Сборка минимального ядра:**
   - Скачать исходники ядра
   - Создать минимальную конфигурацию
   - Собрать и установить ядро

2. **Создание initramfs:**
   - Создать минимальный initramfs
   - Добавить необходимые утилиты
   - Протестировать загрузку

3. **Кастомизация ядра:**
   - Отключить ненужные модули
   - Включить необходимые функции безопасности
   - Оптимизировать размер ядра

## 🔍 Вопросы для самопроверки

1. Какие компоненты необходимы для минимального ядра?
2. В чем разница между ядром и модулями ядра?
3. Что такое initramfs и зачем он нужен?
4. Как можно уменьшить размер ядра?
5. Какие функции безопасности можно включить в ядро?
6. Как отлаживать проблемы с ядром?
7. В чем разница между монолитным и модульным ядром?

## 📚 Дополнительные ресурсы

- [Linux Kernel Documentation](https://www.kernel.org/doc/html/latest/)
- [Kernel Build Guide](https://www.kernel.org/doc/html/latest/process/howto.html)

---

**Статус:** ✅ Базовые материалы созданы  
**Последнее обновление:** 2025

