# Подписание файлов с помощью GPG

## 📚 Описание

Это руководство показывает, как подписывать файлы с помощью GPG для обеспечения целостности и аутентификации.

## 🎯 Цели

- Подписать файл с помощью GPG
- Понять формат подписи
- Создать отдельный файл подписи
- Подписать несколько файлов

## 🚀 Практическое задание

### Шаг 1: Создание тестового файла

```bash
# Создать тестовый файл
echo "Это тестовый файл для подписания" > test_file.txt

# Или использовать существующий файл
# Например, исходный код программы
```

### Шаг 2: Подписание файла (отдельный файл подписи)

```bash
# Создать отдельный файл подписи (.sig)
gpg --detach-sign --armor test_file.txt

# Результат: test_file.txt.asc (файл подписи)
```

**Формат файла подписи:**
```
-----BEGIN PGP SIGNATURE-----
...
-----END PGP SIGNATURE-----
```

### Шаг 3: Подписание файла (встроенная подпись)

```bash
# Создать файл с встроенной подписью
gpg --clearsign test_file.txt

# Результат: test_file.txt.asc (исходный файл + подпись в одном файле)
```

**Формат файла с встроенной подписью:**
```
-----BEGIN PGP SIGNED MESSAGE-----
Hash: SHA256

Это тестовый файл для подписания
-----BEGIN PGP SIGNATURE-----
...
-----END PGP SIGNATURE-----
```

### Шаг 4: Подписание бинарных файлов

```bash
# Для бинарных файлов используйте --detach-sign
gpg --detach-sign --armor binary_file.exe

# Результат: binary_file.exe.asc
```

### Шаг 5: Подписание с указанием ключа

```bash
# Если у вас несколько ключей, укажите нужный
gpg --detach-sign --armor --default-key your-email@example.com test_file.txt

# Или по отпечатку ключа
gpg --detach-sign --armor --default-key ABC123DEF4567890ABCDEF1234567890ABCDEF12 test_file.txt
```

### Шаг 6: Подписание нескольких файлов

```bash
# Подписать все .txt файлы в директории
for file in *.txt; do
    gpg --detach-sign --armor "$file"
done

# Или использовать find
find . -name "*.cpp" -exec gpg --detach-sign --armor {} \;
```

## 📖 Теоретическая часть

### Типы подписей

**1. Отдельная подпись (Detached Signature):**
- Подпись хранится в отдельном файле
- Исходный файл остается неизменным
- Удобно для распространения
- Формат: `filename.asc` или `filename.sig`

**2. Встроенная подпись (Clearsign):**
- Подпись встроена в сам файл
- Файл остается читаемым (текстовый формат)
- Удобно для текстовых файлов
- Формат: `filename.asc`

**3. Бинарная подпись (Binary Signature):**
- Подпись встроена в бинарный файл
- Файл становится бинарным
- Используется редко

### Процесс подписания

```
1. Создание хеша файла (SHA-256)
   └─> Хеш = hash(file_content)

2. Шифрование хеша приватным ключом
   └─> Подпись = encrypt(hash, private_key)

3. Сохранение подписи
   └─> Файл подписи содержит: подпись + метаданные
```

### Зачем нужна подпись?

**Целостность (Integrity):**
- Гарантия, что файл не был изменен
- Любое изменение файла сделает подпись невалидной

**Аутентификация (Authentication):**
- Подтверждение авторства файла
- Только владелец приватного ключа мог создать подпись

**Неотрекаемость (Non-repudiation):**
- Автор не может отрицать создание файла
- Подпись криптографически доказывает авторство

## 🛠 Практические примеры

### Пример 1: Подписание исходного кода

```bash
# Подписать все исходные файлы проекта
cd my_project
for file in src/*.cpp src/*.h; do
    gpg --detach-sign --armor "$file"
    echo "Подписан: $file"
done
```

### Пример 2: Подписание архива

```bash
# Создать архив проекта
tar -czf project.tar.gz project/

# Подписать архив
gpg --detach-sign --armor project.tar.gz

# Результат: project.tar.gz.asc
```

### Пример 3: Подписание с комментарием

```bash
# Добавить комментарий к подписи
gpg --detach-sign --armor --comment "Release v1.0" release.tar.gz
```

## 🔍 Вопросы для самопроверки

1. В чем разница между отдельной и встроенной подписью?
2. Почему подпись становится невалидной при изменении файла?
3. Можно ли подписать файл без приватного ключа?
4. Зачем нужна подпись для распространения программного обеспечения?
5. Что произойдет, если приватный ключ будет скомпрометирован?

## 📚 Дополнительные ресурсы

- [GPG Signing Guide](https://www.gnupg.org/gph/en/manual/x135.html)
- [Code Signing Best Practices](https://cheatsheetseries.owasp.org/cheatsheets/Code_Signing_Cheat_Sheet.html)

---

**Статус:** ✅ Материал создан  
**Последнее обновление:** 2025


