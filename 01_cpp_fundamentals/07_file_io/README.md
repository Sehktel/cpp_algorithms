# Тема 7: Файловый ввод/вывод в C++

## 📚 Описание

Комплексное введение в работу с файлами в современном C++. Файловый I/O - основа для всех инструментов безопасности, работающих с данными: логи, конфигурации, дампы памяти, сетевые пакеты.

## 🎯 Цели обучения

- Работа с текстовыми и бинарными файлами
- Обработка ошибок при работе с файлами
- Оптимизация производительности файлового I/O
- Понимание механизмов буферизации
- Применение файлового I/O в инструментах безопасности

## 🔗 Связи с другими темами

### Предыдущие темы
- [Тема 5: Memory layout](../05_memory_layout/) - понимание размещения данных необходимо для бинарного I/O
- [Тема 6: CLI args](../06_cli_args/) - передача путей к файлам через аргументы командной строки

### Следующие темы
- [Тема 8: TCP Client-Server](../../02_networking/08_tcp_client_server/) - передача файлов по сети
- [Тема 10: FS mirroring](../../04_filesystem/10_fs_mirroring/) - работа с файловой системой
- [Тема 13: Filesystem Forensics](../../04_filesystem/13_filesystem_forensics/) - анализ файлов в форензике
- [Тема 21: DFIR](../../07_forensics/21_dfir/) - работа с дампами памяти и логами

## 📁 Структура лабораторной работы

```
07_file_io/
├── README.md                    # Этот файл
├── 01_basic_text_io.cpp        # Базовый текстовый I/O
├── 02_binary_io.cpp            # Бинарный I/O
├── 03_error_handling.cpp       # Обработка ошибок
├── 04_advanced_techniques.cpp  # Продвинутые техники
├── 05_stream_buffering.cpp     # Буферизация потоков
├── 06_high_performance_io.cpp  # Высокопроизводительный I/O
├── 07_advanced_error_handling.cpp  # Продвинутая обработка ошибок
├── 08_stringstream_examples.cpp # Строковые потоки
├── CMakeLists.txt              # Конфигурация сборки
└── build.ps1                   # Скрипт сборки для Windows
```

## 🚀 Запуск

### Компиляция

```bash
# Windows (PowerShell)
.\build.ps1

# Или вручную
g++ -std=c++17 -Wall -Wextra -O2 01_basic_text_io.cpp -o 01_basic_text_io.exe

# Linux/macOS
g++ -std=c++17 -Wall -Wextra -O2 01_basic_text_io.cpp -o 01_basic_text_io
```

### Запуск

```bash
# Windows
.\01_basic_text_io.exe

# Linux/macOS
./01_basic_text_io
```

## 📖 Теоретическая часть

### Лабораторная #1: Базовый текстовый I/O

**Изучаемые концепции:**
- `ifstream` и `ofstream` - базовые классы потоков
- RAII (Resource Acquisition Is Initialization) - автоматическое управление ресурсами
- Построчное чтение через `getline()`
- Форматированный ввод/вывод через `operator<<` и `operator>>`
- Проверка состояния потока через `is_open()`

**Ключевые моменты:**
- В C++ файлы автоматически закрываются при выходе из области видимости (деструктор)
- `getline()` предпочтительнее `operator>>` для чтения строк с пробелами
- Всегда проверяйте успешность открытия файла

### Лабораторная #2: Бинарный I/O

**Изучаемые концепции:**
- Режим `ios::binary` - критически важен для бинарных данных
- Методы `read()` и `write()` - неформатированный I/O
- Сериализация POD (Plain Old Data) структур
- Произвольный доступ через `seekg()` и `seekp()`
- `tellg()` и `tellp()` - получение текущей позиции

**Производительность:**
```
Текстовый:  int 12345 → "12345" (5 байт + \n)
Бинарный:   int 12345 → 4 байта напрямую
```

**Предупреждения:**
⚠️ Бинарная сериализация работает только для POD типов!  
⚠️ Проблемы с переносимостью (endianness, padding)  
⚠️ Для сложных объектов используйте Protocol Buffers, JSON или Boost.Serialization

### Лабораторная #3: Обработка ошибок

**Изучаемые концепции:**
- Флаги состояния потока: `goodbit`, `eofbit`, `failbit`, `badbit`
- Методы проверки: `good()`, `fail()`, `bad()`, `eof()`
- Восстановление после ошибок через `clear()` и `ignore()`
- Включение исключений через `exceptions()`
- Безопасная обработка некорректного ввода

**Архитектура состояний:**
```
iostate (битовая маска):
┌─────────┬──────────┬───────────┬──────────┐
│ goodbit │  eofbit  │  failbit  │  badbit  │
│  (0x00) │  (0x01)  │  (0x02)   │  (0x04)  │
└─────────┴──────────┴───────────┴──────────┘
     ✓         EOF      Логическая  Критическая
   Всё OK              ошибка      ошибка
```

### Лабораторная #4: Продвинутые техники

**Изучаемые концепции:**
- Манипуляторы потоков: `setw()`, `setprecision()`, `setfill()`, `fixed`, `hex`
- Настройка буферизации через `rdbuf()->pubsetbuf()`
- Сравнение производительности различных подходов
- CSV файлы - практический пример
- Потоковая обработка больших файлов

**Бенчмарки производительности (100,000 строк):**

| Метод | Время | Оценка |
|-------|-------|--------|
| `endl` (с flush) | ~500-800 мс | ❌ Медленно |
| `\n` (без flush) | ~100-150 мс | ✓ Хорошо |
| Увеличенный буфер | ~80-120 мс | ✓✓ Лучше |
| Блочная запись | ~50-80 мс | ✓✓✓ Отлично |

**Критически важное правило:**
```cpp
// ❌ ПЛОХО - flush на каждой итерации!
for (int i = 0; i < n; i++) {
    file << data << endl;
}

// ✓ ХОРОШО - flush только в конце
for (int i = 0; i < n; i++) {
    file << data << "\n";
}
```

## 🎓 Теоретические основы

### 1. Буферизация

Операционные системы используют многоуровневую буферизацию:

```
Приложение → C++ Stream Buffer → Kernel Buffer → Disk Cache → Диск
   (8KB)           (8KB)              (4KB)         (vary)
```

**Зачем нужна буферизация?**
- Системный вызов (syscall) дорогой: ~1-2 микросекунды
- Запись 1 байта без буфера = 1 syscall
- Запись 1 байта с буфером 8KB = 1 syscall на 8000 операций
- **Ускорение в 1000+ раз!**

### 2. Форматированный vs Неформатированный I/O

**Форматированный (текстовый):**
```cpp
int x = 42;
file << x;  // "42" - преобразование в ASCII
```
- Преимущества: читаемость, переносимость, отладка
- Недостатки: медленнее, больший размер, потеря точности для float

**Неформатированный (бинарный):**
```cpp
int x = 42;
file.write(reinterpret_cast<char*>(&x), sizeof(x));  // 0x2A000000 (little-endian)
```
- Преимущества: скорость, компактность, точность
- Недостатки: нечитаемый, проблемы с endianness

### 3. Архитектура C++ Stream I/O

```
                    ios_base
                       |
                      ios
                    /     \
                   /       \
              istream     ostream
               /   \       /   \
              /     \     /     \
         ifstream  iostream  ofstream
                      |
                   fstream
```

**Иерархия классов:**
- `ios_base` - базовый класс (флаги, состояние)
- `ios` - добавляет буферизацию
- `istream` - операции ввода
- `ostream` - операции вывода
- `iostream` - ввод и вывод
- `ifstream`, `ofstream`, `fstream` - файловые специализации

## 🛠 Практическая часть

### Паттерны и best practices

**1. RAII для гарантированного закрытия:**
```cpp
// ✓ Правильно - автоматическое закрытие
{
    ofstream file("data.txt");
    file << "data";
    // file.close() вызовется автоматически в деструкторе
}
```

**2. Проверка ошибок:**
```cpp
// Уровень 1: Минимальная проверка
ofstream file("data.txt");
if (!file) {
    cerr << "Error opening file\n";
    return;
}
```

**3. Эффективная обработка больших файлов:**
```cpp
// ✓ Хорошо - потоковая обработка
string line;
while (getline(file, line)) {
    process(line);  // O(1) память
}
```

## 🔍 Вопросы для самопроверки

1. В чем разница между текстовым и бинарным режимом открытия файла?
2. Почему `endl` медленнее `\n`?
3. Как обрабатывать файлы размером больше RAM?
4. Какие проблемы безопасности могут возникнуть при работе с файлами?
5. Когда использовать исключения, а когда коды возврата для обработки ошибок?

## 📚 Дополнительные ресурсы

- [The C++ Standard Library by Nicolai M. Josuttis](https://www.josuttis.com/libbook/) - глубокое погружение в iostream
- [Effective Modern C++ by Scott Meyers](https://www.aristeia.com/books.html) - best practices
- [Linux AIO vs io_uring](https://kernel.dk/io_uring.pdf)

---

**Статус:** ✅ Материалы мигрированы  
**Источник:** `file_read_write/` (legacy)  
**Последнее обновление:** 2025

