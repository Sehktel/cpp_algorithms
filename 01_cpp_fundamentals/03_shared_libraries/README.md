# Тема 3: Компилирование и получение динамической библиотеки C++

## 📚 Описание

Динамические библиотеки (shared libraries, shared objects) - это механизм разделения кода между несколькими программами. В контексте кибербезопасности понимание shared libraries критично для:
- Анализа загружаемых модулей в процессах
- Понимания механизмов загрузки вредоносного ПО (DLL injection, LD_PRELOAD)
- Аудита безопасности библиотек
- Разработки плагинов и расширений для инструментов безопасности

## 🎯 Цели обучения

- Понимание процесса компиляции и линковки
- Создание shared library (.so на Linux, .dll на Windows)
- Динамическая загрузка библиотек (dlopen/dlsym)
- Понимание механизма разрешения символов
- Анализ зависимостей библиотек

## 🔗 Связи с другими темами

### Предыдущие темы
- Базовые знания C++ и компиляции

### Следующие темы
- [Тема 4: Сигналы](../04_signals/) - обработка сигналов в динамически загружаемых модулях
- [Тема 9: GDB отладка](../../03_debugging_analysis/09_gdb_disasm/) - отладка загруженных библиотек
- [Тема 12: strace/ltrace](../../03_debugging_analysis/12_strace_ltrace/) - анализ системных вызовов загрузки библиотек
- [Тема 15: Kernel modules](../../05_kernel/15_kernel_module/) - аналогия с загрузкой модулей ядра
- [Тема 21: DFIR](../../07_forensics/21_dfir/) - анализ загруженных библиотек в памяти процесса

## 📁 Структура лабораторной работы

```
03_shared_libraries/
├── README.md                    # Этот файл
├── my_math.h                    # Заголовочный файл библиотеки
├── my_math.cpp                  # Реализация библиотеки
├── calc.cpp                     # Программа, использующая библиотеку
├── Makefile                     # Правила сборки
├── shared_libs.md               # Подробная теория shared libraries
└── linker_internals.md          # Внутреннее устройство линкера
```

## 🚀 Запуск

### Компиляция библиотеки и программы

```bash
# Linux/macOS
make

# Или вручную:
# 1. Создать объектный файл с позиционно-независимым кодом
g++ -std=c++11 -Wall -Wextra -fPIC -c my_math.cpp -o my_math.o

# 2. Создать shared library
g++ -shared -o libmy_math.so my_math.o

# 3. Скомпилировать программу
g++ -std=c++11 -Wall -Wextra -c calc.cpp -o calc.o

# 4. Линковать с библиотекой
g++ calc.o -L. -lmy_math -o calc
```

### Запуск программы

```bash
# Указать путь к библиотеке
export LD_LIBRARY_PATH=.:$LD_LIBRARY_PATH  # Linux
export DYLD_LIBRARY_PATH=.:$DYLD_LIBRARY_PATH  # macOS

# Запустить программу
./calc 10 + 5
./calc 20 - 8
./calc 6 * 7
./calc 15 / 3
```

### Установка библиотеки в систему

```bash
# Установить в /usr/local/lib
sudo make install

# Удалить
sudo make uninstall
```

## 📖 Теоретическая часть

### Типы библиотек

**Статические библиотеки (.a, .lib):**
- Код копируется в исполняемый файл
- Больший размер исполняемого файла
- Нет зависимости во время выполнения

**Динамические библиотеки (.so, .dll, .dylib):**
- Код загружается в память один раз
- Разделение между процессами
- Обновление без перекомпиляции программ

### Процесс загрузки библиотеки

1. **Компиляция:**
   ```cpp
   g++ -fPIC -c my_math.cpp  // Position Independent Code
   ```

2. **Создание shared object:**
   ```bash
   g++ -shared -o libmy_math.so my_math.o
   ```

3. **Линковка:**
   ```bash
   g++ calc.o -L. -lmy_math -o calc
   ```

4. **Загрузка во время выполнения:**
   - Динамический линкер ищет библиотеку в путях (LD_LIBRARY_PATH, /etc/ld.so.conf)
   - Загружает библиотеку в память процесса
   - Разрешает символы (адреса функций)

### Механизм разрешения символов

**Порядок поиска библиотек:**
1. Пути из `LD_LIBRARY_PATH`
2. Кэш `/etc/ld.so.cache` (обновляется через `ldconfig`)
3. Стандартные пути (`/lib`, `/usr/lib`, `/usr/local/lib`)

**Анализ зависимостей:**
```bash
# Показать зависимости программы
ldd calc

# Показать зависимости библиотеки
ldd libmy_math.so

# Показать символы в библиотеке
nm -D libmy_math.so
```

### Безопасность и shared libraries

**Уязвимости:**
- **LD_PRELOAD атаки:** Загрузка вредоносной библиотеки перед стандартными
- **DLL hijacking:** Подмена библиотеки в пути поиска
- **Небезопасные функции:** Использование устаревших функций (например, `strcpy`)

**Защита:**
- Проверка целостности библиотек (GPG подписи)
- Использование `RPATH` вместо `LD_LIBRARY_PATH`
- Аудит загруженных библиотек в процессах

## 🛠 Практическая часть

### Пример: Простой калькулятор

**my_math.h / my_math.cpp:**
- Реализация базовых математических операций
- Демонстрация экспорта функций из библиотеки

**calc.cpp:**
- Использование библиотеки
- Обработка аргументов командной строки
- Обработка ошибок (деление на ноль)

### Практические задания

1. **Динамическая загрузка:**
   - Использовать `dlopen()` и `dlsym()` для загрузки библиотеки во время выполнения
   - Реализовать систему плагинов

2. **Анализ зависимостей:**
   - Использовать `ldd` для анализа зависимостей
   - Написать скрипт для проверки всех загруженных библиотек в системе

3. **Безопасность:**
   - Продемонстрировать LD_PRELOAD атаку
   - Реализовать защиту от подмены библиотек

## 🔍 Вопросы для самопроверки

1. В чем разница между статической и динамической линковкой?
2. Что такое Position Independent Code (PIC)? Зачем он нужен?
3. Как работает механизм разрешения символов?
4. Что такое LD_PRELOAD и как его можно использовать для атак?
5. Как проверить целостность загруженной библиотеки?

## 📚 Дополнительные ресурсы

- **shared_libs.md** - Подробная теория shared libraries
- **linker_internals.md** - Внутреннее устройство линкера
- [Program Library Howto](https://tldp.org/HOWTO/Program-Library-HOWTO/)
- [dlopen(3) manual](https://man7.org/linux/man-pages/man3/dlopen.3.html)

---

**Статус:** ✅ Материалы мигрированы  
**Источник:** `cpp_base/` (legacy)  
**Последнее обновление:** 2025

