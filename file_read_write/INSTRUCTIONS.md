# 🚀 Быстрый старт - Инструкция по работе с лабораторными

## Подготовка окружения (Windows)

### Проверка компилятора

Откройте PowerShell и выполните:

```powershell
g++ --version
```

Если видите версию компилятора - всё готово! Если нет, установите один из вариантов:

1. **MinGW-w64** (рекомендуется): https://www.mingw-w64.org/
2. **MSYS2**: https://www.msys2.org/
3. **Visual Studio** с C++ компонентами

---

## 🎯 Три способа работы с лабораторными

### Способ 1: Автоматический (через PowerShell скрипт)

**Компилировать всё:**
```powershell
.\build.ps1
```

**Скомпилировать и сразу запустить конкретную лабу:**
```powershell
.\build.ps1 1    # Первая лаба
.\build.ps1 2    # Вторая лаба
.\build.ps1 3    # Третья лаба
.\build.ps1 4    # Четвёртая лаба
```

**Запустить все программы:**
```powershell
.\build.ps1 run
```

**Очистить всё (бинарники и файлы данных):**
```powershell
.\build.ps1 clean
```

---

### Способ 2: Ручная компиляция (PowerShell)

**Скомпилировать одну программу:**
```powershell
g++ -std=c++17 -Wall -Wextra -O2 01_basic_text_io.cpp -o 01_basic_text_io.exe
```

**Запустить:**
```powershell
.\01_basic_text_io.exe
```

**Скомпилировать все разом:**
```powershell
Get-ChildItem *.cpp | ForEach-Object { 
    $name = $_.BaseName
    g++ -std=c++17 -Wall -Wextra -O2 $_.Name -o "$name.exe"
    if ($?) { Write-Host "✓ $name.exe" -ForegroundColor Green }
}
```

---

### Способ 3: Через CMake (профессиональный)

**Настроить проект:**
```powershell
mkdir build
cd build
cmake ..
```

**Собрать:**
```powershell
cmake --build .
```

**Запустить все лабораторные:**
```powershell
cmake --build . --target run_all
```

**Запустить конкретную программу:**
```powershell
.\bin\01_basic_text_io.exe
```

**Очистить данные:**
```powershell
cmake --build . --target clean_data
```

---

## 📋 Порядок прохождения

Рекомендую изучать лабораторные последовательно:

### 1️⃣ Базовый текстовый I/O (01_basic_text_io.cpp)
**Что изучим:**
- Открытие и закрытие файлов
- Запись и чтение текста
- RAII идиома
- Построчное чтение

**Ключевые концепции:** `ofstream`, `ifstream`, `getline()`

---

### 2️⃣ Бинарный I/O (02_binary_io.cpp)
**Что изучим:**
- Бинарные vs текстовые файлы
- Сериализация структур
- Произвольный доступ
- `read()` и `write()`

**Ключевые концепции:** `ios::binary`, `seekg()`, `seekp()`, POD types

---

### 3️⃣ Обработка ошибок (03_error_handling.cpp)
**Что изучим:**
- Состояния потоков (goodbit, failbit, badbit, eofbit)
- Проверка и восстановление после ошибок
- Исключения vs коды возврата
- Робастное чтение конфигов

**Ключевые концепции:** `good()`, `fail()`, `clear()`, `exceptions()`

---

### 4️⃣ Продвинутые техники (04_advanced_techniques.cpp)
**Что изучим:**
- Манипуляторы потоков
- Оптимизация производительности
- Буферизация
- Работа с большими файлами

**Ключевые концепции:** `setw()`, `setprecision()`, буферы, streaming

---

## 💡 Советы по изучению

### Активное обучение
1. **Не просто читайте код** - запускайте каждую программу
2. **Смотрите на созданные файлы** - откройте их в Notepad++/VS Code
3. **Экспериментируйте** - меняйте параметры и смотрите, что получится
4. **Ломайте код** - специально создавайте ошибки, чтобы понять поведение

### Эксперименты

**После прохождения каждой лабы попробуйте:**

**Лаба 1:**
- Создайте программу для чтения CSV файла
- Напишите простой дневник (добавление записей с датой)

**Лаба 2:**
- Добавьте новое поле в структуру Student
- Реализуйте поиск студента по ID
- Попробуйте сериализовать `std::string` (hint: сначала длину, потом данные)

**Лаба 3:**
- Создайте конфиг с синтаксической ошибкой и проверьте обработку
- Добавьте валидацию диапазонов (например, GPA должен быть 0-5)

**Лаба 4:**
- Сравните производительность с разными размерами файлов
- Попробуйте обработать реально большой файл (100 МБ+)
- Создайте свой манипулятор потока

---

## 🐛 Отладка

### Если компиляция не удалась

**Проверьте путь к компилятору:**
```powershell
where.exe g++
```

**Проверьте версию C++:**
```powershell
g++ -std=c++17 --version
```

**Если ошибка "g++ not recognized":**
1. Установите MinGW/MSYS2
2. Добавьте путь к `bin` в PATH:
   ```powershell
   $env:Path += ";C:\mingw64\bin"  # Замените на ваш путь
   ```

### Если программа не запускается

**Проверьте наличие .exe файла:**
```powershell
ls *.exe
```

**Запустите с правами администратора** (если антивирус блокирует)

**Проверьте, не заблокирован ли файл:**
```powershell
Unblock-File .\01_basic_text_io.exe
```

### Если странное поведение программы

**Удалите старые файлы данных:**
```powershell
.\build.ps1 clean
```

**Запустите заново:**
```powershell
.\build.ps1 1
```

---

## 📊 Проверка результатов

После запуска каждой программы проверьте созданные файлы:

**Лаба 1 создаёт:**
- `output.txt` - можно открыть блокнотом и прочитать

**Лаба 2 создаёт:**
- `students.dat` - бинарный файл (будет нечитаемый в блокноте - это нормально!)

**Лаба 3 создаёт:**
- `test_data.txt`
- `config.ini`
- `test_format.txt`

**Лаба 4 создаёт:**
- `formatted_output.txt` - красиво отформатированный вывод
- `perf_test1.txt` ... `perf_test4.txt` - для теста производительности
- `data.csv` - CSV файл
- `large_file.txt` - большой файл для стресс-теста

**Просмотр файлов:**
```powershell
# Текстовый файл
Get-Content output.txt

# Первые 10 строк
Get-Content large_file.txt -Head 10

# С цветным выводом (если установлен bat)
bat output.txt
```

---

## 🎓 Дополнительные задания

После прохождения всех лабораторных попробуйте реализовать:

### Мини-проекты (2-4 часа каждый)

1. **Лог-анализатор**
   - Читает лог-файл
   - Извлекает ошибки
   - Создаёт отчёт по количеству ошибок каждого типа

2. **Простая БД записной книжки**
   - Структура: Имя, Телефон, Email
   - Команды: add, search, delete, list
   - Данные в бинарном файле

3. **Конвертер форматов**
   - CSV ↔ JSON
   - Обработка больших файлов через streaming

4. **Компрессор текста**
   - RLE (Run-Length Encoding) алгоритм
   - Сравнение размеров до/после
   - Подсчёт степени сжатия

5. **Генератор отчётов**
   - Читает данные из CSV
   - Форматированный вывод с таблицами
   - Экспорт в красиво оформленный текстовый файл

---

## 📖 Дальнейшее изучение

### Книги
- **"C++ Primer" (5th ed)** - глава про I/O
- **"The C++ Standard Library"** by Josuttis - полное руководство
- **"Effective Modern C++"** - best practices

### Online ресурсы
- [cppreference.com/w/cpp/io](https://en.cppreference.com/w/cpp/io) - документация
- [learncpp.com](https://www.learncpp.com/) - уроки
- [Compiler Explorer](https://godbolt.org/) - смотрите сгенерированный ассемблер

### Следующие шаги
После освоения базового файлового I/O изучите:
- **Сериализация:** Protocol Buffers, JSON libraries (nlohmann/json)
- **Многопоточность:** работа с файлами из нескольких потоков
- **Сетевое программирование:** сокеты, HTTP
- **Базы данных:** SQLite, встраиваемые БД

---

## ❓ Часто задаваемые вопросы

**Q: Можно ли использовать Visual Studio Code?**  
A: Да! Откройте папку в VS Code, установите расширение C/C++, и используйте встроенный терминал для компиляции.

**Q: Почему не создаётся .exe файл?**  
A: Проверьте ошибки компиляции. Возможно, не установлен g++ или есть синтаксические ошибки.

**Q: Программа вылетает при запуске**  
A: Скорее всего, антивирус блокирует. Добавьте папку в исключения.

**Q: Как посмотреть бинарный файл?**  
A: Используйте hex-редактор (HxD для Windows) или команду:
```powershell
Format-Hex students.dat | Select-Object -First 20
```

**Q: Зачем нужны все эти флаги компиляции (-Wall, -Wextra)?**  
A: Это включает дополнительные предупреждения компилятора, которые помогают находить потенциальные ошибки.

---

## 🎯 Критерии успешного прохождения

✅ Все 4 программы компилируются без ошибок  
✅ Все программы запускаются и завершаются нормально  
✅ Вы понимаете, что делает каждая строка кода  
✅ Вы можете объяснить разницу между текстовым и бинарным I/O  
✅ Вы знаете, как обрабатывать ошибки при работе с файлами  
✅ Вы можете написать простую программу для работы с файлами самостоятельно  

---

## 📞 Получение помощи

- **Комментарии в коде** - содержат подробные объяснения
- **README.md** - теоретическая база и примеры
- **Этот файл** - практические инструкции

**Удачи в изучении! 🚀**

---

*Последнее обновление: 2025*


